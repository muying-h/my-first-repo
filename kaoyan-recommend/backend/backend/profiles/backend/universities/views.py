python<br>from rest_framework.decorators import api_view<br>from rest_framework.response import Response<br>from .models import University<br>from .serializers import UniversitySerializer<br><br>@api_view(['GET'])<br>def list_universities(request):<br> qs = University.objects.all()<br> region = request.query_params.get('region')<br> level = request.query_params.get('level')<br> if region:<br> qs = qs.filter(region=region)<br> if level:<br> qs = qs.filter(level=level)<br> return Response(UniversitySerializer(qs, many=True).data)<br>
