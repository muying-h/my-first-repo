python<br>import logging<br><br>logger = logging.getLogger('recommend')<br><br>class RuleRecommendEngine:<br> """规则推荐引擎（非AI降级方案）"""<br><br> def score_university(self, profile, university, major, admission):<br> score = 0<br> reasons = []<br> risks = []<br><br> # 层次匹配<br> if university.level == '985':<br> score += 30<br> reasons.append('院校层次为985')<br> elif university.level == '211':<br> score += 20<br> reasons.append('院校层次为211')<br> else:<br> score += 10<br> reasons.append('院校层次为双非')<br><br> # 地域匹配<br> if profile.preferred_region and profile.preferred_region in university.region:<br> score += 25<br> reasons.append(f'地域符合意向（{university.region}）')<br><br> # 分数匹配度<br> try:<br> rank = int(''.join(filter(str.isdigit, profile.rank_percent.split('%')[0])))<br> except Exception:<br> rank = 50<br> if rank <= 20:<br> score += 30<br> reasons.append('成绩排名靠前，匹配度较高')<br> elif rank <= 50:<br> score += 20<br> reasons.append('成绩排名中等，匹配度适中')<br> else:<br> score += 10<br> risks.append('成绩排名偏后，冲刺风险较高')<br><br> # 报录比处理（实验3要求：缺失不杜撰）<br> if admission.pending or not admission.ratio:<br> risks.append('报录比待核实，请以官网为准')<br> else:<br> reasons.append(f'报录比{admission.ratio}')<br><br> return score, '；'.join(reasons), '；'.join(risks)<br><br> def recommend(self, profile, candidates):<br> results = []<br> for university, major, admission in candidates:<br> score, reason, risk = self.score_university(profile, university, major, admission)<br> results.append({<br> 'university': university.name,<br> 'major': major.name,<br> 'score_line': admission.score_line,<br> 'ratio': admission.ratio or '待核实',<br> 'reason': reason,<br> 'risk': risk,<br> 'pending': admission.pending,<br> '_score': score,<br> })<br> results.sort(key=lambda x: x['_score'], reverse=True)<br> return results<br>
